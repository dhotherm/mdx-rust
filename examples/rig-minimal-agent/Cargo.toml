[package]
name = "rig-minimal-agent"
version = "0.1.0"
edition = "2021"
license = "MIT"
publish = false

[dependencies]
rig.workspace = true
tokio = { version = "1", features = ["full"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
anyhow = "1"
